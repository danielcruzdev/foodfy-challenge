{% extends "layoutAdmin.njk" %}

{% block content %}

<main>
    <div class="detail-header">
        <div class="detail-text-container">
            <h3>Receita: {{ recipe.title }}</h3>
            <div class="button-edit">
                <a href="/admin/recipes/{{ recipe.id }}/edit">Editar receita</a>
            </div>    
        </div>
        <div class="detail-image-container">
            <img src="{{recipe.image}}" class="detail-image-container" />
        </div>
        <div class="detail-info-container">
            <h2>{{recipe.title}}</h2>
            <p>por {{recipe.author}}</p>
        </div>
    </div>
    <div class="details-recipe">
        <div class="details-info">
            <div class="title-button-ingredients detail-title-button">
                <h2>Ingredientes</h2>
                <a>ESCONDER</a>
            </div>
            <div class="detail-content">
                <ul>
                    {% for ingredient in recipe.ingredients %}
                    <li>{{ingredient}}</li>
                    {% endfor %}
                </ul>
            </div>
        </div>
        <div class="details-info">
            <div class="title-button-preparation detail-title-button">
                <h2>Modo de preparo</h2>
                <a>ESCONDER</a>
            </div>
            <div class="detail-content">
                <ol>
                    {% for step in recipe.preparation %}
                    <li>{{step}}</li>
                    {% endfor %}
                </ol>
            </div>
        </div>
        <div class="details-info">
            <div class="title-button-information detail-title-button">
                <h2>Informações adicionais</h2>
                <a>ESCONDER</a>
            </div>
            <div class="detail-content">
                <p>{{ recipe.information }}</p>
            </div>
        </div>    
    </div>
</main>

<script src="/scripts.js"></script>

{% endblock %}